import { FlexPositions } from "../shared/flexPositions";
import { TextTransformType } from "./textTransformType";

type LegendStyleProp = string | boolean | FlexPositions | number | number[] | TextTransformType;

export default LegendStyleProp;
