import { FlexPositions } from "../shared/flexPositions";
import { TextTransformType } from "./textTransformType";

type LegendStylingProp = string | boolean | FlexPositions | number | number[] | TextTransformType;

export default LegendStylingProp;
